import React, { Component } from "react";
import { connect } from "react-redux";

export default class Home extends Component {
  render() {
    return (
      <div>
        <h2>Enter the name of your favorite marvel hero!</h2>
      </div>
    );
  }
}
